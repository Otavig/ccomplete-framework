#ifndef DCFOR_H
#define DCFOR_H

/**
 * Prints the elements of a character array, each on a new line.
 * 
 * @param list A pointer to the character array to be printed.
 * @param size The number of elements in the array (length of the `list`).
 */
void dcfor(char *list, int size);

#endif
